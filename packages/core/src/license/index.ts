/**
 * Licensing seam.
 *
 * Nothing in Anchor is gated today. This module exists so that if a paid tier
 * is ever added, it is an addition rather than a refactor — and so the shape of
 * that decision is written down before it is made, in docs/PAID-TIER.md.
 */

export * from './entitlements.js';
export * from './verify.js';
