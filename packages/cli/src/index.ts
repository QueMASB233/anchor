/**
 * @eleva/anchor — the Anchor CLI.
 *
 * A thin wrapper over @eleva/anchor-core. All design system logic lives in
 * core; this package owns argument parsing, config discovery, and process
 * exit codes only.
 */

export const CLI_VERSION = '0.0.0';

export {};
